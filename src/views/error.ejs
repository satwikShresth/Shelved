<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Error</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background-color: #121212;
            font-family: Arial, sans-serif;
            color: #f5f5f5;
        }

        .error-container {
            text-align: center;
            padding: 2em;
            background-color: #1e1e1e;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.6);
            max-width: 400px;
            width: 100%;
        }

        h1 {
            font-size: 3em;
            color: #e74c3c;
            margin: 0;
        }

        h2 {
            font-size: 1.5em;
            margin-top: 0.5em;
            color: #f5f5f5;
        }

        p {
            font-size: 1em;
            color: #bbb;
            margin: 1em 0;
        }

        .icon {
            font-size: 4em;
            margin-bottom: 1em;
        }

        button {
            padding: 0.5em 1em;
            background-color: #ff9800;
            color: #fff;
            border: none;
            border-radius: 4px;
            font-size: 1em;
            cursor: pointer;
            margin: 0.5em;
        }

        button:hover {
            background-color: #e68900;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="icon">⚠️</div> // add icon to make the error more prominent
        <h1><%= status %></h1>
        <h2><%= error %></h2>
        <p><%= message %></p>
        <button onclick="window.location.href='/'">Go to Home</button>
        <button onclick="window.history.back()">Go Back</button> // added a go back button
    </div>
</body>
</html>
